import type { ButtonProps, ButtonSize, ButtonVariant } from "./button.svelte"
import Root, {

  buttonVariants,
} from "./button.svelte"

export {
  //
  Root as Button,
  type ButtonProps,
  type ButtonSize,
  type ButtonVariant,
  buttonVariants,
  type ButtonProps as Props,
  Root,
}
